[workspace]
resolver = "2"
members = [
    "hypervisor",
    "tools/compat-check",
    "tools/aether-install",
]

[profile.release]
lto = true
opt-level = "s"
panic = "abort"

[profile.dev]
panic = "abort"
