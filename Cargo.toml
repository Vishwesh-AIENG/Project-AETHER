[workspace]
resolver = "2"
members = [
    "hypervisor",
    "tools/compat-check",
    "tools/aether-install",
    "aether-translator",
]

exclude = [
    "aether-translator/fuzz",
]

[profile.release]
lto = true
opt-level = "s"
panic = "abort"

[profile.dev]
panic = "abort"
